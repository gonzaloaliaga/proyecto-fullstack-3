apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: donation-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: donation-db-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: donation-db
  template:
    metadata:
      labels:
        app: donation-db
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - name: POSTGRES_DB
              value: "donation-db"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: donaton-global-secrets
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: donaton-global-secrets
                  key: DB_PASSWORD
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: donation-db-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: donation-db-storage
          persistentVolumeClaim:
            claimName: donation-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: donation-db
spec:
  selector:
    app: donation-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
